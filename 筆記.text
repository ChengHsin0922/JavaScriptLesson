0617
--語法區--
1.method後請加()
2.console.time()開始+console.timeEnd()結尾:計算運算時間
3.物件=屬性+方法，使用屬性或是方法:物件.屬性，物件.方法()
4.如果js有直接存取到body內的元素則，則寫在head的js無法生效
    必須將使用引用js檔的方式，並將script標籤加上defer屬性
5.on...代表事件
6.const宣告的變數不能修改，let宣告的變數如果寫在括號裡面，"作用域"只存在括號裡面(主流)
7.if(判斷|比較|布林){要做的事}
8.x = 5=>x設值5，x == 5=>比較x是否等於數字5
9.var重複宣告會採用後蓋前，let不能重複宣告
--語法區--
